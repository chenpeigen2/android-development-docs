# 文档更新审阅记录

- **审阅日期**：2026-09-09
- **审阅基线**：`860f48f...d28daf9`
- **审阅范围**：基于 AOSP `android-17.0.0_r1` 的 Android 技术文档更新。

## 审阅结论

本次更新的主体是技术正文：窗口与 Surface、View 绘制、输入系统、Binder、Handler、Activity 启动、SystemUI、插件化、性能、安全、网络、Compose 及跨平台文档均保留了调用链、源码入口、实现边界和示例。

审阅发现并已处理以下问题：

1. `ACCESS_LOCAL_NETWORK` 不再被写成“所有 targetSdk 37 应用都必然需要的固定运行时权限”，改为根据 SDK、设备版本、targetSdk 和访问路径判断。
2. 不再把 `external/conscrypt/nsc` 和 `RootTrustManager` 的内部目录/实现关系写成稳定应用契约。
3. “Android 17 新无锁 MessageQueue”补充了 AOSP 路径，并明确这是平台实现细节，不是 targetSdk 37 应用可依赖的公开 API。
4. 修复了插件化和输入系统文档尾部截断、未闭合代码示例问题。
5. `README.md` 已同步更新时间和更新日志。

## 校验结果

- `git diff --check`：通过。
- 修改文档代码围栏：未发现未闭合围栏。
- 未跟踪的 `build/`：未纳入文档提交。
- 未把本审阅记录伪装成技术章节；它仅作为修订记录保存。

## 仍需注意

Markdown 目录中部分历史章节锚点来自旧版编号，预览器可能无法跳转到重写后的章节。正文技术内容不受影响，但后续应按当前标题重新生成目录链接。
